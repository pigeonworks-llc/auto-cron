// Phase A placeholder so tsc / dep-cruiser have at least one source file
// to operate on. Phase B replaces / augments this with the real entity
// + port skeleton; this constant survives as the runtime version banner
// (printed by `auto-cron --version` once Phase G.2-equivalent CLI lands).
export const VERSION = "0.0.1";
